{
  "description": "A basic Node.js Express server with templating",
  "run_script": "# In order for our node project to run, we need\n# to make sure to install its dependencies:\nnpm install\n\n# And then we can run the \"start\" command:\nnpm start\n",
  "default_collapse": "node_modules"
}
